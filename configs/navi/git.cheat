% git:ff

# Git generic fast-forward 
git pull --ff-only <remote> <branch>

% git:ffm

# Git fast-forward main from generic remote
git pull --ff-only <remote> main

% git:ffo

# Git fast-forward a branch from origin
git pull --ff-only origin <branch>

% git:ffob

# Git fast-forward current branch from origin
git pull --ff-only origin $(git branch --color=never | sed 's:\* ::')

% git:ffom

# Git fast-forward origin main
git pull --ff-only origin main
